<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.kordelle.flachwitze.flachwitze">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NeuesteWitze" />
        <activity android:name=".ueber" />
        <activity android:name=".Entwickler" />
        <activity android:name=".btom" />
        <activity android:name=".berik" />
        <activity android:name=".termine" />
        <activity android:name=".hiersindwir" />
        <activity android:name=".mainpage2" />
        <activity android:name=".WenigWitze" />
        <activity android:name=".Handy" />
        <activity
            android:name=".login"
            android:label="@string/title_activity_login" />
        <activity android:name=".mainpageli" />
        <activity android:name=".Copyright" />
        <activity android:name=".Web"></activity>
    </application>

</manifest>